// Every guardian in the game. A run fights all of them, shuffled, one in every
// other chamber (rooms.js), so adding a boss here also lengthens the run by
// two chambers. No imports: rooms.js reads its length while loading.
export const BOSS_POOL = ['turtle', 'croc', 'gorilla', 'peacock', 'vesper', 'naga', 'solaris', 'aldric', 'bride', 'warden', 'monkey', 'maestro'];
